allprojects {
    group = 'org.hibernate.models'
    version = '1.0-SNAPSHOT'
    
    repositories {
        mavenCentral()
    }
}

subprojects {
    apply plugin: 'java'
    apply plugin: 'java-library'
    
    ext {
        ormVersion = "7.0.9.Final"
        //ormVersion = "7.2.0.CR2"
    }
}